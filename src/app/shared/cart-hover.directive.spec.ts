import { CartHoverDirective } from './cart-hover.directive';

describe('CartHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new CartHoverDirective();
    expect(directive).toBeTruthy();
  });
});
